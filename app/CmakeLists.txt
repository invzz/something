cmake_minimum_required(VERSION 3.29)


find_package(raylib CONFIG REQUIRED)



set(SINGLE single)
set(MULTI multi)

include_directories(inc)

add_executable(${MULTI} src/multi.cc )
target_link_libraries(${MULTI} PRIVATE raylib  )

add_compile_definitions(-DRES_DIR="${RES_DIR}")
add_compile_definitions(-DAPP_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
add_compile_definitions(-DSHADER_DIR="${PROJECT_SOURCE_DIR}/shaders/")
add_compile_definitions(-DTEXTURE_DIR="${PROJECT_SOURCE_DIR}/textures/")
add_compile_definitions(-DUSE_NVIDIA)
add_compile_definitions(-DPLATFORM_DESKTOP)



